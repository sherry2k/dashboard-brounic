# Public assets

Upload the Brounic logo here as:

```text
public/logo.png
```

Next.js serves files from this folder at the site root, so `public/logo.png` is available in the browser as `/logo.png`.
