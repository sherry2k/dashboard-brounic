import ProjectManager from "@/components/ProjectManager";

export default function SupplyPage() {
  return (
    <ProjectManager
      type="supply"
      title="New Project"
      subtitle="Supply and Installation"
      description="Track FACP, PVC piping, device installation, fire pumps, shop drawings and more — progress rises as each task is ticked off. Click any row to view details in a popup."
    />
  );
}
